#!/usr/bin/env  ruby

# creates a variable named types_of_people and sets it at 10
types_of_people = 10
# creates another variable that has a string and uses the previos variable
x = "There are #{types_of_people} types of people."
# creates a variable that is a string
binary = 'binary'
# creates a variable that is a string
do_not = "don't"
# creates another variable that has a string and uses the previos variable
y = "Those who know #{binary} and those who #{do_not}."

# puts the variable we created earlier
puts x
# puts the variable we created earlier
puts y

# puts a new string plus a variable (X) that we created earlier
puts "I said: #{x}."
# puts a new string plus a variable (y) that we created earlier

# puts a new string plus a variable (X) that we created earlier
puts "I also said: '#{y}'."

# sets the variable 'hilarious' to false
hilarious = false
# creates new variable that has a string and the variable we just created
joke_evaluation = "Isn't that joke so funny?! #{hilarious}"

# puts the variable we just created
puts joke_evaluation

# creates two new variables with strings
w = 'This is the left side of...'
e = 'a string with a right side.'

# puts those variables
puts w + e
